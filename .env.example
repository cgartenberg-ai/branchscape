# Copy this file to ".env" (same folder) and put your real key in it.
#   cp .env.example .env   then edit .env
# .env is gitignored and will NEVER be committed. The council_server loads it
# automatically on startup, so you don't have to `export` the key each session.
ANTHROPIC_API_KEY=sk-ant-your-key-here
